#include "main.h"

/**
 * _abs - computes the absolute value of an integer number
 * @num: the number to be evalutated
 * Return: the absolute value of the number
 */
int _abs(int num)
{
	if (n >=0)
	{
		return (num);
	}
	return (-num);
}
