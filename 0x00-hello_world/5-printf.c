#include <stdio.h>

/**
 * main - prints a continuation of the previous statement.
 * Return: 0 if all is well.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
